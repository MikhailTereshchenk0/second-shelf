spring:
  datasource:
    password: ${DB_PASSWORD:secret}

app:
  security:
    public-docs-enabled: true

internal:
  token: ${INTERNAL_TOKEN:internal-secret-123}

jwt:
  secret: ${JWT_SECRET:change_this_secret_to_something_long_and_random_1234567890_change_me}

auth:
  refresh-token:
    pepper: ${AUTH_REFRESH_TOKEN_PEPPER:local-dev-refresh-token-pepper-1a1f645d5a1f4a09b1d769e942f3a6cf}
