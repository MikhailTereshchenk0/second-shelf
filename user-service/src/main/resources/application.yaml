server:
  port: ${USER_SERVICE_PORT:8081}

spring:
  application:
    name: user-service
  profiles:
    default: local

  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${POSTGRES_PORT:5432}/${USER_DB_NAME:users_db}
    username: ${DB_USERNAME:second_shelf}
    password: ${DB_PASSWORD:}

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    show-sql: false

  liquibase:
    change-log: classpath:/db/changelog/db.changelog-master.yaml

internal:
  token: ${INTERNAL_TOKEN:}

seed:
  admin:
    username: ${SEED_ADMIN_USERNAME:}
    password: ${SEED_ADMIN_PASSWORD:}
    email: ${SEED_ADMIN_EMAIL:}

app:
  security:
    public-docs-enabled: false

jwt:
  secret: ${JWT_SECRET:}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true
      show-details: never
  health:
    livenessstate:
      enabled: true
    readinessstate:
      enabled: true

springdoc:
  api-docs:
    enabled: ${app.security.public-docs-enabled:false}
  swagger-ui:
    enabled: ${app.security.public-docs-enabled:false}
    path: /swagger-ui.html
    operations-sorter: method
    tags-sorter: alpha

logging:
  pattern:
    level: "%5p [corr:%X{correlationId:-}]"
